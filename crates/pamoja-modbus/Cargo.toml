[package]
name = "pamoja-modbus"
description = "Modbus RTU framing for pamoja: CRC-16/Modbus, the RTU ADU envelope, the standard request PDUs, and response decoding, so a long-cable RS485 field sensor speaks Modbus, no_std and allocation-free. The framing half ahead of the serial driver."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
keywords.workspace = true
categories.workspace = true
authors.workspace = true

[dev-dependencies]
# Property tests feed the ADU parser arbitrary bytes off the RS485 boundary: it must
# never panic on hostile input, and every built frame must survive a parse round trip.
proptest = "1"

[lints]
workspace = true
