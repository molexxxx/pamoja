# pamoja for C#

The C# reference is listed on [the reference page](https://pamoja.molex.cloud/docs/reference/dotnet.html):
every package with its install line and its API pages. This root is replaced by a
hand-off to that page when the site is assembled.
