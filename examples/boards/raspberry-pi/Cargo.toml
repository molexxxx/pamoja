[package]
name = "board-raspberry-pi"
version = "0.0.0"
edition = "2021"
publish = false
license = "MIT"
description = "Programs for a Raspberry Pi: a sensor on the header's I2C bus, soil probes on its 1-Wire bus, a relay and a switch on its GPIO lines, a pan and tilt head from a PCA9685 and a stepper, a LoRa radio on its SPI bus, a whole profile-driven node, and a dashboard of the LoRaWAN nodes a network server hears."

[dependencies]
pamoja-actuators = { path = "../../../crates/pamoja-actuators" }
pamoja-codec = { path = "../../../crates/pamoja-codec" }
pamoja-core = { path = "../../../crates/pamoja-core" }
pamoja-dashboard = { path = "../../../crates/pamoja-dashboard" }
pamoja-gateway = { path = "../../../crates/pamoja-gateway" }
pamoja-gpio = { path = "../../../crates/pamoja-gpio", features = ["std"] }
pamoja-hal = { path = "../../../crates/pamoja-hal", features = ["linux"] }
pamoja-kit = { path = "../../../crates/pamoja-kit" }
pamoja-lora = { path = "../../../crates/pamoja-lora" }
pamoja-mqtt = { path = "../../../crates/pamoja-mqtt" }
pamoja-profile = { path = "../../../crates/pamoja-profile" }
pamoja-radios = { path = "../../../crates/pamoja-radios", features = ["linux"] }
pamoja-sensors = { path = "../../../crates/pamoja-sensors", features = ["linux"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }

[workspace]
