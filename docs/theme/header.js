// Generated by `cargo xtask docs` from crates/xtask/src/theme.rs.
(function () {
  var title = document.querySelector(".menu-title");
  if (!title || title.dataset.pamoja) {
    return;
  }

  var root = typeof path_to_root === "string" ? path_to_root : "";
  var mark = document.createElement("img");
  mark.src = root + "assets/pamoja-logo.svg";
  mark.alt = title.textContent.trim();
  mark.className = "pamoja-mark";

  var home = document.createElement("a");
  home.href = root + "index.html";
  home.appendChild(mark);

  title.textContent = "";
  title.dataset.pamoja = "1";
  title.appendChild(home);
})();
